# BrickRadar Android

Native Android-app (Kotlin/Compose) mot BrickRadar Flask-API:t (`C:\mould-king-tracker`), som redan har full funktionsparitet med webb-UI:t under `/api/` (se `fas1-brickradar-json-api.md` i det projektet).

## Serverkonfiguration
- `app/src/main/java/com/sivan/brickradar/network/ApiConfig.kt` läser `BASE_URL`/`API_KEY` från `BuildConfig.API_BASE_URL`/`BuildConfig.API_KEY` — **inte längre hårdkodat i källkoden** (ändrat 2026-07-28, se issue #2 i Sivan87/BrickRadar). Den ursprungliga hårdkodade nyckeln (beslut 2026-07-27, dokumenterat här som "avsiktligt") visade sig sitta kvar oroterad i git-historiken sedan initial-commiten — en secrets-scan av repot hittade den, den roterades, och lagringen flyttades till `BuildConfig` för att undvika samma problem igen.
- De faktiska värdena kommer från `app/build.gradle.kts`s `buildConfigField(...)`, som i sin tur läser Gradle-properties `API_BASE_URL`/`API_KEY` via `project.findProperty(...)` — **exakt samma mönster som `RELEASE_STORE_PASSWORD` m.fl.** (se "Release build" nedan). Saknas någon av dem kastar Gradle-buildet ett tydligt fel istället för att tyst falla tillbaka på ett gammalt/hårdkodat värde.
  - **Lokalt:** lägg `API_BASE_URL=http\://<lan-ip>\:5000/` och `API_KEY=<nyckeln>` i root-projektets `gradle.properties` (gitignorad, samma fil som keystore-lösenorden — fylls i manuellt, skickas aldrig genom chatten). Kolonteckenet i URL:en behöver `\:`-escapas i properties-filsyntax.
  - **I CI** (`.github/workflows/release.yml`): skickas som `-PAPI_BASE_URL="${{ secrets.API_BASE_URL }}" -PAPI_KEY="${{ secrets.API_KEY }}"` till `assembleRelease`, satta som repo-secrets i Sivan87/BrickRadar (samma mönster som `KEYSTORE_BASE64`/`KEYSTORE_PASSWORD` m.fl.).
- Flyttar/byter servern IP: uppdatera `API_BASE_URL` i `gradle.properties` (lokalt) och/eller repo-secreten `API_BASE_URL` (CI) OCH IP-adressen i `app/src/main/res/xml/network_security_config.xml` (cleartext HTTP är annars blockerat av Android sedan API 28).
- Roterar API-nyckeln: uppdatera `API_KEY` i `mould-king-tracker/.env` OCH i `gradle.properties`/repo-secreten här samtidigt — de måste vara identiska, servern har bara EN giltig nyckel åt gången.

## Arkitektur (Fas 2 — grundskelett, utökad i Fas 3, Fas 4, Fas 6 och Fas 7)
- `network/` — Retrofit-interface (`BrickRadarApi`) + klient (`RetrofitClient`, lägger till `X-API-Key`-headern via en OkHttp-interceptor på varje request). `BrickRadarApi` har nu även `updateStatus` (`PATCH .../status`), `updateModel` (`PUT /models/{id}`), (Fas 4) `addSource`/`updateSource`/`deleteSource` (`POST/PUT/DELETE .../sources`) och `setSourceOverride`/`deleteSourceOverride` (`POST/DELETE .../source-override`) — de fyra sistnämnda returnerar `okhttp3.ResponseBody` (svarskroppen läses aldrig, se ModelRepository nedan om varför) och stängs alltid direkt med `.close()` — samt (Fas 6) `addModel` (`POST /models`, returnerar den skapade modellen direkt, ingen omhämtning behövs). Fas 7: `searchBrick4ByNumber` (`GET /api/brick4/search-by-number?model_number=`, fanns redan i `mould-king-tracker` — se nedan) och `updateCategory` (`PUT /models/{id}` med en delad, smalare body — se `ModelUpdateRequest.kt`).
- `model/` — datamodeller som speglar JSON-strukturen från `/api/models` och `/api/categories` (fältnamn verifierade mot live-svar från servern, inte bara källkoden). `ModelUpdateRequest.kt` har request-bodyerna för modell-uppdatering, samt (Fas 7) `CategoryUpdateRequest` (bara `category`-fältet — `_apply_model_fields` i `api.py` accepterar ett delvis fältset, så `ModelRepository.addModel`s uppföljande kategori-PUT slipper skicka med name/piece_count den kanske inte känner till än). `SourceRequests.kt` (Fas 4) har `AddSourceRequest`/`UpdateSourceRequest`/`SourceOverrideRequest`. `AddModelRequest.kt` (Fas 6, Fas 7) har request-bodyn för `POST /models` — `name`/`piece_count` är **nullable** sedan Fas 7 (servern accepterar båda tomma och fyller dem i via `initial_fetch`; sökflödet skickar dem tomma om användaren inte själv rättat dem). `Brick4SearchResult.kt` (Fas 7) speglar svaret från `GET /api/brick4/search-by-number` (en rad per märke med exakt modellnummerträff — INTE en rad per set, innehåller inget namn/bild/delantal, se avvikelse-anmärkningen nedan). `Constants.kt` har `CURRENCIES`/`COUNTRIES` (Fas 4), samt (Fas 6) `UNCATEGORIZED_KEY` (flyttad hit från `ModelDetailScreen`, delas nu med `AddModelScreen`) och `KNOWN_BRANDS` (samma märkeslista som webbens "+ Ny modell"-datalist i `templates/index.html`, plus `"Generic"` — det fria textvärde `app.py: _is_manual_only_source` känner igen som "inget riktigt märke" för MOC-liknande set).
- `repository/ModelRepository.kt` — anropar API:t, omvandlar nätverksfel/HTTP-fel till `ApiResult<T>` (sealed: `Success`/`Error`). 400-svar försöker läsa serverns `{"error": "..."}`-body för ett specifikt felmeddelande (t.ex. "Ogiltig kategori"), annars generiskt "Ogiltig data"; 409 (Fas 6, dubblettkontroll vid modelltillägg) läses på samma sätt, annars "Modellen finns redan". `getModels(status, category, valueRating, sort)` fanns redan sedan Fas 1/2 med rätt query-parameternamn (verifierade mot `api.py: api_list_models`) — Fas 5 (filter/sortering i listvyn) behövde alltså bara koppla ihop befintlig repository-metod med UI, ingen ändring i repository/API-lagret. **Viktigt om `sort`:** servern stöder bara `kr_asc`/`kr_desc`/`recent_asc`/`recent_desc` — det finns inget serverstöd för namn-sortering, så `SortOption.NAME_ASC` sorteras klientsidan i `ModelListViewModel.loadModels()` efter att svaret kommit tillbaka. **Fas 4 — viktig backend-detalj:** `PUT /api/sources/{id}` är bekräftat append-only (`prices`-tabellen, se `database.py: add_price`/`get_latest_prices` i `mould-king-tracker`) — en "redigering" sparar en NY prisrad med NYTT id, den gamla raden lever kvar i historiken. Dessutom returnerar `POST/PUT/DELETE .../sources` bara den råa (oberikade) prislistan, utan `kr_per_piece`/`best_kr_per_piece`. Lösning: `addSource`/`updateSource`/`deleteSource` i repository struntar helt i dessa svarskroppar och gör alltid ett omhämtat `GET /api/models/{id}` efteråt för korrekt state (nytt id, uträknat kr/del). Vidare är **leveranstid (`delivery_estimate`) inte alls samma lagringsplats som pris/valuta/URL/lagerstatus/lagerland** — den ligger i en separat tabell (`model_source_overrides`, nyckel modell-id+källnamn) och sätts/tas bort via ett helt annat anrop (`POST/DELETE .../source-override`), samma mekanism som webb-UI:ts separata "Lagerstatus/lagerland/leveranstid"-dialog använder (inte prisdialogen). Appens käll-formulär visar båda i EN sheet för bättre UX, men `addSource`/`updateSource` gör vid behov TVÅ serveranrop internt (prisrad + ev. override) innan den slutgiltiga omhämtningen. **Fas 6 — två bekräftade avvikelser i `POST /models` (verifierade mot `api.py: api_add_model` innan implementation, inte antagna från kickoff-dokumentet):** (1) `brand` är obligatoriskt server-side ("brand krävs", 400 annars) — detta fält saknades helt i kickoff-dokumentets formulärlista, så `AddModelScreen` har ett eget "Märke"-fält (fritext + `KNOWN_BRANDS`-chips) som inte efterfrågades explicit i kickoffen. (2) Ett `category`-fält i body:n till `POST /models` **ignoreras helt** av servern — den sätter alltid `category = suggest_category(name)` (enkel nyckelordsgissning på namnet), oavsett vad klienten skickar. `ModelRepository.addModel` hanterar detta genom att alltid skapa modellen först och därefter, OM användaren valt en annan kategori än "Gissa från namn" (`AddModelScreen`s `AUTO_CATEGORY`-sentinel, skickas som `category = null` till repository), göra ett uppföljande `PUT /models/{id}` (Fas 7: `updateCategory`/`CategoryUpdateRequest`, se nedan — INTE längre samma fulla `ModelUpdateRequest` som redigeringsläget i Fas 3) för att sätta den valda kategorin (inkl. `UNCATEGORIZED_KEY` för "ingen kategori").

**Fas 7 — Brick4-sökning vid modelltillägg, viktig avvikelse från kickoff-dokumentet (verifierad mot `mould-king-tracker/scraper.py`/`api.py` innan implementation):** kickoff-dokumentet (`fas7-brickradar-brick4-sokning.md`) antog att en sökning på modellnummer skulle ge tillbaka namn/bild/delantal/märke per kandidat, redo att visas som förhandsvisningskort innan modellen skapas. Det stämmer INTE mot vad backend faktiskt levererar: `GET /api/brick4/search-by-number` (redan existerande sedan tidigare, `api.py:701`, återanvänds oförändrad — **inget backend-arbete i `mould-king-tracker` behövdes**, till skillnad från vad kickoffen förutsåg) returnerar bara `brand_id`/`brand_name`/`item_id`/`title2url`/`setnumber_id` — EN rad per märke som har en exakt modellnummerträff, inte en rad per set, och inget namn/bild/delantal. Fullständiga detaljer (namn/bild/delantal) kräver ett kompletterande anrop (`scraper._brick4_fetch_set_details`, kräver ett KÄNT märke) som idag bara körs server-side, asynkront, EFTER att `POST /api/models` skapat raden (`app.initial_fetch`) — exakt samma mönster som webbens befintliga "+ Ny modell"-formulär redan använder (`static/app.js: resolveBrandThenCreateModel` — sökningen används bara för att disambiguera märket, inte för en förhandsvisning). Beslut (bekräftat med användaren): Android mirrorar detta webb-beteende istället för att bygga ett nytt backend-endpoint för en riktig förhandsvisning. Konkret betyder det: `AddModelScreen`s bekräftelsesteg efter ett kandidatval visar BARA modellnummer+märke (redan kända från sökningen) — namn/delantal är valfria fält som fylls i av `initial_fetch` i bakgrunden om de lämnas tomma, precis som webbens formulär (`templates/index.html`: "fylls i automatiskt om du lämnar tomt"). Se även **bildöverskrivnings-fällan**: `initial_fetch`/`fetch_and_save_info` har INGEN skydd mot att skriva över ett `image_url` satt vid skapande (till skillnad från namn/delantal/kategori, som redan har sådant skydd) — relevant om en framtida version låter användaren välja en annan bild än den Brick4/Merlins Bricks senare hittar automatiskt; idag sätter Android-formuläret aldrig `image_url` i sökflödet så det är inte ett observerat problem än, bara en känd fallgrop att komma ihåg.
- `viewmodel/` — `ModelListViewModel`, `ModelDetailViewModel` och (Fas 6) `AddModelViewModel`, exponerar UI-state via `StateFlow`. `ModelDetailViewModel` har `updateStatus(newStatus)`, `updateModel(name, pieceCount, category)` och (Fas 4) `addSource(...)`/`updateSource(...)`/`deleteSource(sourceId)`, plus en `events: SharedFlow<ModelDetailEvent>` för engångshändelser (Snackbar-meddelanden vid lyckad/misslyckad sparning) — separat från `uiState` eftersom events inte ska visas igen vid konfigurationsändring. `ModelDetailEvent.SourceSaved` är en egen händelse (utöver `Saved`/`Failed`) som stänger käll-bottensheeten på lyckad spara/redigera, frikopplat från modell-redigeringens `isEditing`-flagga. Fas 5: `ModelListViewModel` har ett eget `filters: StateFlow<ModelListFilters>` (status/kategori/`SortOption`) separat från `uiState` — filterläget ska överleva oberoende av om listan just nu visar Loading/Loaded/Error. `setStatusFilter`/`setCategoryFilter`/`setSortOption` avbryter (`Job.cancel()`) ett ev. pågående `loadModels()`-anrop innan nästa skickas (`loadJob`-referens), och `ModelListUiState.Loaded` har ett `isRefreshing`-flagga så föregående lista syns kvar (med en tunn `LinearProgressIndicator`) istället för att blinka till en tom/laddar-skärm vid filterbyte. Fas 6: `AddModelViewModel` har `categories`/`isSaving`/`events: SharedFlow<AddModelEvent>` (`Created`/`Failed`, samma engångshändelse-mönster som `ModelDetailViewModel`) och `saveModel(...)` som anropar `ModelRepository.addModel`. Fas 7: `AddModelViewModel` har utökats med `mode: StateFlow<AddModelMode>` (`SEARCH` default/`MANUAL`) och `search: StateFlow<Brick4SearchState>` (`query`/`isSearching`/`results`/`error`/`selected`) separat från formulärfälten (som fortfarande ligger som lokalt Compose-state i `AddModelScreen`, oförändrat sedan Fas 6). `searchBrick4(modelNumber)` anropar `ModelRepository.searchBrick4ByNumber`; `selectBrick4Candidate`/`clearBrick4Selection` växlar bekräftelsesteget fram/tillbaka. `saveModel(...)` tar nu `name: String?`/`pieceCount: Int?` (nullable) istället för de tidigare obligatoriska typerna, eftersom sökflödet kan spara utan dem (se avvikelse-anmärkningen i Arkitektur-avsnittet).
- `ui/` — `ModelListScreen.kt` (lista, `LazyColumn`), `ModelDetailScreen.kt` (detaljvy) och (Fas 6) `AddModelScreen.kt` (nytt formulär), navigation via `navigation-compose` i `MainActivity.kt` (routes: `modelList`, `modelDetail/{modelId}`, `addModel`). Detaljvyn har `Scaffold` med `TopAppBar` (pennikon togglar redigeringsläge) och `SnackbarHost`. Fas 4: käll-listan har nu redigera-/ta bort-ikoner per rad, en "+ Lägg till källa"-knapp, ett enhetligt `ModalBottomSheet`-formulär (`SourceFormSheet`, delat mellan lägg till/redigera) och en `AlertDialog` för borttagningsbekräftelse. Valuta/lagerland väljs via `FilterChip`-rader (samma mönster som kategori-väljaren i redigeringsläget) snarare än en dropdown-meny, för konsekvens med resten av appen. Fas 5: `ModelListScreen.kt` har fått en `FilterBar` högst upp — en horisontellt scrollbar `FilterChip`-rad för status (Alla/Sök/Bevakar/Äger/Avslagen) och en till för kategori (byggd från `GET /api/categories`, inkl. "Alla kategorier"), plus en `SortMenuButton` (ikonknapp + `DropdownMenu`) för sortering. Tomt filterresultat visar ett eget meddelande (`EmptyResultView`) istället för en tom lista. Fas 6: `ModelListScreen` har nu ett eget `Scaffold` med en `FloatingActionButton` ("+", navigerar till `addModel`) och `SnackbarHost`. Resultatet av ett lyckat modelltillägg skickas tillbaka via standardmönstret för Navigation Compose: `AddModelScreen`s `onSaved`-callback sätter `MODEL_CREATED_KEY = true` på `navController.previousBackStackEntry?.savedStateHandle` (i `MainActivity`) innan `popBackStack()`; `ModelListScreen` tar emot detta som en `modelCreatedFlow: StateFlow<Boolean>`-parameter (istället för att t.ex. hämta om listan blint på varje återkomst till skärmen, vilket hade ändrat det redan dokumenterade beteendet för tillbaka-navigering från detaljvyn) och triggar `viewModel.loadModels()` + en bekräftelse-Snackbar bara när flaggan faktiskt är satt, sätter sedan tillbaka den till `false` (`onModelCreatedConsumed`) så den inte triggar om vid t.ex. skärmrotation. `AddModelScreen` har ett formulär (Namn/Modellnummer/Märke/Delantal/Kategori/Status/Bildlänk) med samma validerings- och `FilterChip`-mönster som `ModelDetailScreen`s redigeringsläge; kategori-chippen inkluderar ett eget "Gissa från namn"-alternativ (default, se `AUTO_CATEGORY`-anmärkningen ovan) utöver de riktiga kategorierna och "Ingen kategori". Fas 7: `AddModelScreen` har fått ett läge-val högst upp (`FilterChip`-par "Sök modellnummer"/"Fyll i manuellt"). I sökläget före ett val: ett modellnummerfält + sökknapp (`Icons.Filled.Search`), och beroende på `Brick4SearchState` — laddningsindikator, felmeddelande + "Fyll i manuellt istället"-knapp (byter läge och förifyller modellnumret), "inga träffar"-meddelande med samma fallback-knapp, eller en lista `Card`-rader (`Brick4CandidateCard`, en per märkeskandidat) med en "Använd denna"-knapp var. Efter ett val: en `Card` visar det bekräftade modellnumret+märket samt en "Ändra märke"-knapp (går tillbaka till kandidatlistan utan att söka om), och SAMMA formulärfält som det manuella läget återanvänds (`AddModelFields`, en delad privat composable) men med namn/delantal som valfria (ingen valideringsspärr på Spara) och modellnummer/märke-fälten dolda (redan kända, inte redigerbara i detta steg).
- Bildladdning: Coil (`io.coil-kt:coil-compose`) — **inte** med i ursprungliga kickoff-dokumentets beroendelista, men tillagd eftersom listan/detaljvyn behöver visa `image_url` och ingen bildladdningskod fanns specificerad.
- `androidx.compose.material:material-icons-core` — tillagd i Fas 3 för redigera-/avbryt-ikonerna (`Icons.Default.Edit`/`Close`), inte med i ursprungliga beroendelistan. Fas 4 återanvänder samma beroende för `Icons.Default.Add`/`Delete`. `ModalBottomSheet`/`Switch`/`AlertDialog` (Fas 4) kommer redan från `androidx.compose.material3:material3`, ingen ny dependency behövdes.

## Fas 8 — Visuell redesign (Klonradarn Design Options)
Källa: ett claude.ai/design-projekt (`Klonradarn Design Options.dc.html`, importerat via `claude_design`-MCP:t) — en 11-rond designutforskning (t1-t11, ~30 mockup-varianter: färgscheman, list/rutvy, detaljvy, foldbar-stöd) som konvergerar på ett mörkt tema från rond t5 och framåt. "Klonradarn" var mockuparnas arbetsnamn, inte en omdöpning — appen heter fortfarande BrickRadar (bekräftat med användaren, `strings.xml: app_name` oförändrad).
- `ui/theme/` (nytt) — `Color.kt` (exakt hex-palett från designdokumentet: bg `#0D0D0D`/`#0B0D0F`, kort `#0E1113`/highlight `#101316`, guld-accent `#E8C93F`, grön/röd `#63C98A`/`#E0705E`), `Type.kt`, `Theme.kt` (`BrickRadarTheme`, ersätter `MaterialTheme` i `MainActivity`). **Typsnitts-avvikelse:** designdokumentet använder Google Fonts 'Space Grotesk'/'JetBrains Mono`, men `GrotesqueFont`/`MonoFont` i `Type.kt` mappar till `FontFamily.SansSerif`/`FontFamily.Monospace` (systemtypsnitt) istället för att bädda in de riktiga typsnittsfilerna — en nedladdningsbar-typsnitt-leverantör hade krävt ett certifikat-arrayresurs som inte gick att verifiera korrekt utan enhetstest denna session. Byt ut mot riktiga `Font(R.font....)`-familjer om/när faktiska `.ttf`-filer läggs till i `res/font`.
- Nya server-integrationer (endpoints fanns redan i `mould-king-tracker`, oanvända av appen innan Fas 8): `GET /api/stats` (`StatsResponse` — bara `counts` och `avg_kr_per_piece_clone_all`/`_lego_all` modelleras, resten av svaret ignoreras) och `DELETE /api/models/{id}` (`ModelRepository.deleteModel`). Stats driver dels filterchipsens räknare (Alla/Bevakar/Äger m.fl.) i `ModelListScreen`, dels värde-skalans "Klonsnitt"/"LEGO-snitt"-referenspunkter i `ModelDetailScreen` (`ValueScaleSection` — normaliserar klonsnitt/LEGO-snitt/"detta set" till en 0-1-skala med lite marginal, ingen serverstödd skalpositionering).
- `ModelListScreen.kt`: `ListHeader` (wordmark + list/rutvy-växlare, `ListViewMode` i `ModelListViewModel`), kort med helbild-banner + gradient-scrim + värde/bit + delta%-pill (från `priceTrend.pct`, redan i `Model`), rutvy-variant (`ModelGridCard`, 2 kolumner), 4-flikars bottennav (`Sets`/`Lägg till`/`Statistik`/`Mer` — se Fas 10 nedan, `Statistik` navigerar numera till en riktig skärm, `Mer` renderas fortfarande medvetet nedtonad/inaktiv; ersätter den gamla FAB:en). Glyfer (≡/▦/◎/⟳) är ren text, inte vektor-ikoner — undviker beroende på `material-icons-extended` som inte finns i projektet.
- `ModelDetailScreen.kt`: contain-skalad bildpanel (ingen beskärning), värde/bit-hero med statusmärke, `ValueScaleSection` (grön→guld→röd-gradient med Klonsnitt/LEGO-snitt-markörer + "Detta set"-pekare), källtabell med kr/del-kolumn och guldkant på billigaste källan (redigera/ta bort-ikonerna bevarade från Fas 4), tomt prishistorik-läge (ingen historik-endpoint kopplad än), verktygsrad ("+ Lägg till pris manuellt", återanvänder befintligt käll-formulär). **Ny funktionalitet, inte bara omfärgning:** en kontextuell åtgärdsrad ("Flytta till bevakning"/"Avslå"/"Ta bort", synlig bara när `status == "new"`) där "Ta bort" raderar hela modellen permanent via det nya `deleteModel`-anropet, med bekräftelsedialog och `ModelDetailEvent.Deleted` som navigerar tillbaka till listvyn (`MainActivity`: `ModelDetailScreen(onDeleted = { navController.popBackStack() })`).
- `AddModelScreen.kt`: enbart omfärgad (samma fält/validering/flöde som Fas 6/7), `FilterChip` → tema-anpassad `FormPillChip`, `Card` → `FormCard`.
- **Inte implementerat från designdokumentet:** foldbar-stöd (rond t11 — Galaxy Z Fold-anpassat tvåpanelsläge med bredd-brytpunkt och dragbar delare) — ett uttryckligt scope-beslut med användaren, dokumentet självt beskriver det som ett tillägg ovanpå t10-basen snarare än en förutsättning för den.
- **App-ikon:** riktig launcher-ikon (kugghjul/≠, källfil ursprungligen `Designer.png`) ersätter Android-standardroboten, som adaptiv ikon (förgrund: kugghjulet, transparent, centrerat inom Androids säkerhetszon; bakgrund: `@color/ic_launcher_background` = `#0D0D0D`, ny färg i `values/colors.xml`). Projektets `mipmap-anydpi`-mapp (ingen `-v26`-suffix i det här projektet, ofarligt eftersom `minSdk = 26` redan är golvet — adaptiv ikon är alltså allt som någonsin laddas) pekade tidigare på tomma Android Studio-standardvektorer (`@drawable/ic_launcher_background`/`ic_launcher_foreground`) — dessa är borttagna, `ic_launcher.xml`/`ic_launcher_round.xml` pekar nu istället mot `@mipmap/ic_launcher_foreground` (samma PNG i alla dpi-mappar) + färgresursen. De gamla legacy `.webp`-ikonerna (mdpi/hdpi/xhdpi/xxhdpi/xxxhdpi, för <API 26, aldrig faktiskt använda här) är också borttagna för att undvika en dubblett-resurskonflikt med de nya `ic_launcher.png`/`ic_launcher_round.png`-filerna i samma mappar.

## Fas 9 — Auto-update (in-app uppdateringskoll)
Appen kollar nu själv om det finns en nyare version, istället för att varje uppdatering krävt manuell överföring+installation av en ny APK (se "Release build"/"Så här uppdaterar du appen" ovan, som fortfarande gäller för själva byggsteget — Fas 9 automatiserar bara distributionen/installationsprompten därefter).
- **Byggdes ursprungligen mot en egen server-endpoint (`mould-king-tracker`s `GET /api/app-version`/`version.json`/`publish-update.py`), men byter 2026-07-28 (issue #1 i Sivan87/BrickRadar) till att anropa GitHub Releases direkt** — se `network/GitHubReleasesApi.kt`/`RetrofitClient.githubApi` nedan och "Så här uppdaterar du appen" längre ner för den fullständiga bakgrunden/motiveringen. Servern har inte längre någon roll i uppdateringskollen alls.
- `network/GitHubReleasesApi.kt`: `getLatestRelease()` (`GET https://api.github.com/repos/Sivan87/BrickRadar/releases/latest`, ingen autentisering — publikt repo) → `GitHubReleaseResponse` (`model/GitHubRelease.kt`: `tagName`/`body`/`assets`). Går via en EGEN `Retrofit`-instans (`RetrofitClient.githubApi`, egen `OkHttpClient` utan `apiKeyInterceptor`n) — `X-API-Key` ska aldrig skickas till GitHub, den hör bara hemma på anrop mot vår egen server. `repository/ModelRepository.kt`: `getAppVersion()` bygger nu själv en `AppVersionResponse` (`model/AppVersion.kt`, oförändrad form) av GitHub-svaret — `versionCode` parsas ur `versionName`s sista `.`-segment (samma tolkning som serverns gamla `_fetch_latest_github_release`/`api_app_version` gjorde), `downloadUrl` blir asset-objektets egna `browser_download_url`. Samma `safeCall`/`ApiResult`-mönster som resten av repositoryt — **fail-silent-kravet hanteras fortfarande av anroparen** (`UpdateViewModel` ignorerar `ApiResult.Error` helt), inte av repositoryt/`safeCall` självt. `BrickRadarApi.getAppVersion()` (den gamla server-endpointen) är borttagen, ingen kod anropar den längre.
- `viewmodel/UpdateViewModel.kt` — **`AndroidViewModel`, inte den vanliga `ViewModel`-basklassen** appens övriga ViewModels ärver (`ModelListViewModel` m.fl.), eftersom `DownloadManager`/`FileProvider`/`PackageManager` alla kräver en `Context`; `application`-context räcker (ingen `Activity`-referens hålls, ingen läckrisk vid rotation). Fungerar ändå med samma `viewModel()`-composable-funktion som resten av appen redan använder (Compose Navigations default `SavedStateViewModelFactory` kan instansiera `AndroidViewModel`-subklasser reflektivt via en `(Application)`-konstruktor, ingen egen `ViewModelProvider.Factory` behövdes).
  - `checkForUpdate()` körs en gång i `init` (dvs. en gång per appstart, inte per skärmbesök): jämför svarets `versionCode` mot `BuildConfig.VERSION_CODE`. **Kräver `buildFeatures { buildConfig = true }`** i `app/build.gradle.kts` — avstängt som default sedan AGP 8, annars finns `BuildConfig`-klassen inte alls och koden kompilerar inte (bekräftat, första `assembleDebug`-försöket denna session gav `Unresolved reference 'BuildConfig'` tills flaggan lades till).
  - `startDownload()`: `DownloadManager.Request` mot `downloadUrl`, sparas som `brickradar-update.apk` i `getExternalFilesDir(Environment.DIRECTORY_DOWNLOADS)` (`setDestinationInExternalFilesDir`). En ev. tidigare nedladdad fil på samma sökväg raderas INNAN en ny nedladdning startas, så ett omtryck på "Uppdatera" (t.ex. efter att just ha beviljat installationsbehörighet) aldrig råkar installera en gammal/ofullständig fil.
  - Nedladdningsförlopp: `DownloadManager`s egen inbyggda notis (`VISIBILITY_VISIBLE_NOTIFY_COMPLETED`) + en enkel obestämd (`CircularProgressIndicator`) in-app-dialog medan `UpdateDownloadState.Downloading` — ingen exakt procentandel läses ut ur `DownloadManager.Query` (hade krävt polling; bedömdes som onödig komplexitet för en engångs-nedladdning på lokalt WiFi).
  - En `BroadcastReceiver` (registrerad via `ContextCompat.registerReceiver(..., ContextCompat.RECEIVER_NOT_EXPORTED)` — **inte** den råa 4-parameters `Context.registerReceiver`-overloaden, som kräver API 33+; `ContextCompat`s variant fungerar redan från `minSdk = 26`) lyssnar på `DownloadManager.ACTION_DOWNLOAD_COMPLETE`, verifierar att det är RÄTT nedladdnings-id (flera nedladdningar skulle annars kunna krocka) och sätter `ReadyToInstall`/`Failed` beroende på `DownloadManager.COLUMN_STATUS`.
  - `installApk()`: `FileProvider.getUriForFile` (auktoritet `${applicationId}.fileprovider`, se manifest/`res/xml/file_paths.xml` nedan) + `Intent(ACTION_VIEW)` med `application/vnd.android.package-archive`-mimetyp — **aldrig** en rak `file://`-URI (blockerad av Android sedan API 24). Nollar `updateInfo` efter en lyckad `startActivity` så "Ny version tillgänglig"-dialogen inte dyker upp igen bakom installationsskärmen om användaren avbryter — appen frågar på nytt vid nästa vanliga appstart.
  - `canInstallPackages()`: `packageManager.canRequestPackageInstalls()`.
- `ui/UpdateDialog.kt` (`UpdateChecker`-composable, anropad en gång från `MainActivity`, ovanpå `NavHost`): tre `AlertDialog`-varianter beroende på state — "Ny version tillgänglig" (visar `versionName`+`releaseNotes`, knappar Uppdatera/Senare), en förklarande "Tillåt installation"-dialog INNAN `Settings.ACTION_MANAGE_UNKNOWN_APP_SOURCES` öppnas (bara om `canInstallPackages()` är false när användaren trycker Uppdatera) via `rememberLauncherForActivityResult`, och nedladdnings-/feldialoger. **Kollar den faktiska behörigheten igen efter returnen** från inställningssidan istället för att lita på launcherns resultatkod — `ACTION_MANAGE_UNKNOWN_APP_SOURCES` returnerar `RESULT_CANCELED` på en del enheter även när användaren faktiskt beviljade behörigheten.
- `AndroidManifest.xml`: `android.permission.REQUEST_INSTALL_PACKAGES` (krävs sedan API 26 för att appen själv ska få starta en installations-intent utan att användaren slår på "okända källor" globalt) + en `FileProvider`-deklaration (`${applicationId}.fileprovider`, `exported=false`) som pekar mot `res/xml/file_paths.xml` (nytt: en `<external-files-path name="update_downloads" path="Download/" />`, matchar exakt den mapp `setDestinationInExternalFilesDir(..., DIRECTORY_DOWNLOADS, ...)` skriver till — exponerar INTE hela lagringsutrymmet).
- **Publicera en ny version**: se "Så här uppdaterar du appen" nedan — sedan 2026-07-28 räcker en vanlig `git push` till `main`, ingen manuell server-publicering (`publish-update.py`, borttaget) längre. Uppdateringsdialogen dyker upp automatiskt nästa appstart, oavsett vilket nätverk telefonen är på (kräver inte längre hemma-WiFi, se nedan).
- **Inte implementerat**: ingen exakt nedladdningsprocent (se ovan), ingen bakgrundskoll (kollar bara vid appstart, inte periodiskt medan appen är öppen), inget sätt att manuellt trigga en omkoll utan att starta om appen.

## Fas 10 — Statistik-flik (version 1: Klon- vs LEGO-snitt)
Bottennavens tredje flik hette tidigare "Priser" och var medvetet inaktiv (se Fas 8) — den är nu ett riktigt "Statistik"-flik/skärm istället för att fortsätta peka mot en obefintlig vy.
- `ui/StatistikScreen.kt` (ny) + `viewmodel/StatistikViewModel.kt` (ny, samma `Loading`/`Loaded`/`Error`-mönster som övriga ViewModels) — anropar samma redan befintliga `GET /api/stats` (`ModelRepository.getStats()`, ingen ny endpoint/DTO-utökning behövdes) och visar `avgKrPerPieceCloneAll`/`avgKrPerPieceLegoAll` (samma fält som redan användes av `ModelDetailScreen`s `ValueScaleSection`) som två kort, "Klon-snitt"/"LEGO-snitt", i samma mörka tema/guld-accent/`MonoFont` som resten av appen.
- **Backend-exklusionen av Generic/MOC ur klon-snittet var redan klar innan detta jobb påbörjades** — verifierat direkt mot `mould-king-tracker/api.py` (`_is_generic_or_moc_brand`, filtrerar bort `brand` "generic"/"moc" ur `avg_kr_per_piece_clone_all`): ingen dubbelkoll eller ändring behövdes i den här kickoffen, det var inte en öppen fråga.
- `ModelListScreen.kt`s `BottomNavBar`: `BottomNavItem`-raden för "Priser" är omdöpt till "Statistik" och `enabled` satt till `true`, med ett nytt callback-parameter `onStatistikClick` (samma trådnings-mönster som `onAddModelClick`) som `MainActivity.kt` kopplar till `navController.navigate("statistik")`. "Mer"-fliken är oförändrad (fortsatt `enabled = false`).
- **Inte med i version 1** (medvetet avgränsat, kan läggas till som separata features senare): statusfördelning (new/watching/owned/rejected-räkningar, redan i `StatsResponse.counts` men oanvänt här), kategori-snitt (`category_breakdown`, oanvänt i DTO:n), totalt investerat värde/samlingsvärde (`total_pieces_owned`/`estimated_collection_value_sek`, oanvänt i DTO:n) — `StatsResponse` (`model/Stats.kt`) modellerar fortfarande bara en delmängd av det verkliga `/api/stats`-svaret, se kommentaren där för hela fältlistan servern faktiskt returnerar.

## Fas 11 — Statistik fullt paritet + värdeskale-fix + borttagen dödknapp (issues #4, #5, #6 i Sivan87/BrickRadar, 2026-07-28)

Tre separata, av användaren rapporterade buggar, alla åtgärdade i samma jobb:

- **Issue #4 ("Snitt baren skalningen ändras")** — `ModelDetailScreen.kt`s `ValueScaleSection` ritade tidigare en generisk 3-stopps grön→guld→röd-gradient (`ScaleGreen`/`ScaleGold`/`ScaleRed`, nu borttagna) utsträckt mellan MIN/MAX av vilka tre värden (detta sets kr/del, Klonsnitt, LEGO-snitt) som råkade finnas — samma position kunde alltså hamna i en annan färg beroende på vilka andra modeller som fanns i databasen, trots att kr/del-chippen på källrader/listkort redan alltid färgas efter de FASTA trösklarna i `app.py: classify_value`/`CLONE_VALUE_LEVELS`/`LEGO_VALUE_LEVELS` (se `mould-king-tracker/CLAUDE.md`, "Value scoring (kr/piece)"). Ny fil `util/ValueRating.kt` speglar dessa trösklar exakt (klon: cyan ≤0.35/grön ≤0.55/gul ≤0.80/orange ≤1.00/annars röd; LEGO: grön ≤0.70/gul ≤1.00/orange ≤1.30/annars röd, ingen cyan-nivå) och exponerar `classifyValue`/`colorForValueRating`. Bakgrundsgradienten byggs nu som ett SEGMENTERAT band (hårda kanter vid varje verkligt tröskelvärde i kr/del, inte en jämn interpolering) via `Brush.horizontalGradient(colorStops = ...)`, med domänen (0 till `maxV`) skalad för att rymma både de faktiska värdena och hela den tillämpliga trappan (klon- eller LEGO-skalan, valt via `model.isOfficialSet`). "Detta set"-markören/texten färgas nu med `model.bestValueRating` (serverns egna, frakt-justerade sanning) istället för alltid vit — ny färg `ValueCyan`/`ValueGreen`/`ValueYellow`/`ValueOrange`/`ValueRed` i `Color.kt`, hex-värden verifierade mot `static/style.css`s `--cyan`/`--green`/`--amber`/`--orange`/`--red`. Verifierat live (emulator, modell "Ford GT" #10651, samma modell som i buggrapportens skärmdump): "Detta set 0.32" visas nu i cyan (matchar dess riktiga `best_value_rating`), och bakgrunden är ett tydligt 5-färgat band istället för en jämn 3-färgad gradient.
- **Issue #5 ("Sök knapp som inte ska finnas")** — den read-only `StatusBadge(status = model.status)`-pillen bredvid VÄRDE/BIT-rubriken i `ModelDetailScreen.kt` (en ren `Text`, inte klickbar — men uppfattades av användaren som en obrukbar knapp) togs bort helt tillsammans med sin nu oanvända composable. Statuslägesbyte sker fortfarande via de riktiga, klickbara `StatusChipsRow`-pillerna längre ner (Sök/Bevakar/Äger/Avslagen) — ingen funktionalitet togs bort, bara den överflödiga icke-interaktiva badgen. `Row`-wrappern runt VÄRDE/BIT-kolumnen förenklades till en ren `Column` eftersom den bara hade ett barn kvar. Verifierat live: badgen syns inte längre, resten av headern (bild/namn/delantal/VÄRDE-BIT) ligger kvar oförändrad.
- **Issue #6 ("statistik sidan innehåller inte mycket info")** — `StatistikScreen.kt` visade tidigare (Fas 10, version 1) bara Klon-/LEGO-snittet. `model/Stats.kt` modellerar nu HELA `GET /api/stats`-svaret (verifierat fältnamn/former direkt mot `api.py: api_stats`/`_category_breakdown`, inte antaget) — nya datklasser `ValueDistribution`, `StatsModelRef` (delad form för `best_deals` OCH `category_breakdown[].best`), `BrandBreakdown`, `CategoryBreakdown`, plus `total_pieces_owned`/`estimated_collection_value_sek`/`avg_kr_per_piece_owned` på `StatsResponse`. Skärmen visar nu, i SAMMA ordning som `static/app.js: renderStats()`: statusfördelning (fyra färgade barer, ny färg `StatusWatchingBlue` matchande webbens `--blue` för "Bevakar" — de andra tre återanvänder redan befintliga `ValueYellow`/`PositiveGreen`/`TextDim`), sex snabbstatistik-kort (Bevakar/Äger/Sök-antal, Delar ägda, Samlingsvärde, Snitt kr/del ägda), Klon-/LEGO-snittet från version 1 (behölls — finns inte på webbens egen statistiksida, men var redan implementerat och togs inte bort), prisläge bland bevakade (värdefördelning cyan/grön/gul/orange/röd med samma `colorForValueRating`), bästa fynd just nu (klickbara rader, navigerar till `modelDetail/{id}` — nytt `onModelClick`-callback trådat genom `MainActivity.kt`), fördelning per märke, och bästa kr/del per kategori (klickbar rad om kategorin har en billigaste modell). Verifierat live (emulator + riktig lokal server, `GET /api/stats` hämtat rått via curl och jämfört fält-för-fält mot vad skärmen faktiskt renderade — alla siffror identiska): statusbarer, alla sex kort, båda snitten, värdefördelningen, samtliga fem bästa-fynd-rader, alla nio märkesrader och alla fem kategorirader stämmer, och att trycka på en bästa-fynd-rad (JieStar #91102) navigerar korrekt till den modellens detaljvy.

## Fas 12 — Designdokumentets rond t11 (foldbar-stöd) + t12 (statistiksida-mockup), 2026-07-29

Designprojektet (`Klonradarn Design Options.dc.html`, samma claude.ai/design-projekt som Fas 8, importerat på nytt via `claude_design`-MCP:t) hade vuxit med en ny rond, t12, sedan Fas 8/10/11 — och t11 (Galaxy Z Fold-stöd) stod kvar som ett medvetet avgränsat "inte implementerat" sedan Fas 8. Båda togs upp i samma jobb, plus en re-verifiering av t1-t10 mot nuvarande kod.

- **t11 (Galaxy Z Fold, tidigare avgränsad — nu implementerad i en pragmatisk, funktionell tolkning, inte pixel-för-pixel):**
  - `ModelListScreen.kt: ModelGrid` — `GridCells.Fixed(2)` (fast 2-kolumners rutnät) bytt mot `GridCells.Adaptive(minSize = 160.dp)` — speglar designens `grid-template-columns: repeat(auto-fill, minmax(178px,1fr))` (rond t11b/t11d: samma rutnät växer automatiskt från 2 kolumner på telefon till fler på en uppfälld Fold (~838dp) eller surfplatta (~1194dp), ingen egen bredd-brytpunkts- eller foldable-specifik detekteringskod behövdes — `GridCells.Adaptive` löser hela "väx med bredden"-kravet på egen hand).
  - `ModelDetailScreen.kt: ModelDetail` — innehållet under `HeroSection` delas nu i två spalter sida vid sida på breda skärmar (`BoxWithConstraints`, brytpunkt `maxWidth >= 600.dp`, samma tröskel som Material's egen compact/medium-gräns): vänster spalt (vikt 1.5f) har status/värdeskala/källor/prishistorik, höger spalt (fast 260dp) har verktyg/fakta — speglar rond t11c:s tvåkolumnslayout (`1fr 260px`-rutnät i mockupen). Under 600dp är beteendet oförändrat (samma enkla stapel som tidigare, matchar 8a/9a). `BottomActionBar` förblir en fullbredds-rad i båda lägena, matchar t11c:s egen fullbredds-footer.
  - **Medvetet INTE gjort pixel-för-pixel:** t11c:s sammanslagna header (bild+namn+värde/bit+statusknappar i EN rad) återskapades inte — `HeroSection`/`StatusChipsRow`/`BottomActionBar` återanvänds oförändrade i båda bredd-lägena, eftersom en full header-ombyggnad hade varit en mycket större, riskfylld omskrivning för samma funktionella resultat (tvåspalts-innehåll på breda skärmar) som redan uppnås utan den. Ingen riktig foldbar-emulator/enhet användes för verifiering denna session (`assembleDebug` + kompilering är det enda som bekräftats, se "Ej verifierat" nedan) — bredd-brytpunkten testades inte live mot en Fold-emulator.
- **t12 (ny rond, "Statistiksida — telefon")** — jämfördes mot `StatistikScreen.kt` (byggd i Fas 10/11 mot webbens `/api/stats`, oberoende av detta designdokument) och visade sig redan innehålla nästan hela strukturen/innehållet. Kvarstående pixel-skillnader fixade:
  - `StatusDistribution`: strukturen ändrad från EN rad (etikett | bar | antal sida vid sida) till TVÅ rader per status (etikett+antal ovanpå en fullbredds-bar) för att matcha mockupens exakta layout. "Sök"-barens färg bytt från `ValueYellow` (#ffce45) till `AccentGold` (#e8c93f) — mockupens exakta hex för just den här baren (till skillnad från `ValueYellow`, som är rätt färg för kr/del-värdering på annat håll i appen, men fel för just detta stapeldiagram).
  - `SectionTitle` (delad av alla fem underrubriker på sidan) — bytt från versal/dämpad stil (`labelMedium`/`TextMutedMore`, versaler) till mockupens egen (fetstil, EJ versaler, ljusare `TextSecondary`) — en konsekvent skillnad som gällde alla fem rubriker, inte bara en.
  - Värdefördelningens bildtext ("bland N bevakade...") bytt färg från `TextMutedMost` till `TextDim` (mockupens exakta `#5f666e`).
  - **Medvetet INTE ändrat:** värdefördelningens punktfärger (kanon/bra/okej/högt/mycket högt) använder fortfarande den delade `colorForValueRating`-funktionen (samma cyan/grön/gul/orange/röd-trösklar som resten av appen, se Fas 11 issue #4 ovan) istället för mockupens egna, något avvikande exempel-hexvärden (t.ex. `#8fd6a8` för "bra") — att byta till mockupens specifika svit hade brutit den medvetna, redan en gång fixade konsekvensen med resten av apples kr/del-färgsättning för en engångs-mockups exempeldata. Toppfältets fasta "Klonradarn + guldpill"-header (utan tillbaka-pil) återskapades inte heller — `StatistikScreen` nås via navigation (inte en ren bottennav-flik i mockupens mening) och behöver en riktig tillbaka-väg, så den befintliga `TopAppBar` med tillbaka-pil behölls.
- **t1-t10 re-verifiering** — en fristående granskning läste varje rond direkt ur designfilen (inte bara CLAUDE.md:s egna påståenden) och diffade mot koden. t1-t4 (ljust tema/strukturexploration) är, som redan dokumenterat, korrekt INTE speglade i koden — de ströks av t5:s mörka-tema-konvergens. t5 stämmer exakt. t6:s färgade värdeskale-gradient är en MEDVETEN avvikelse (redan förklarad under Fas 11 ovan, issue #4/#7/#8) — inte drift. Granskningen hittade dock verklig, tidigare odokumenterad avdrift i t7/t9/t10, samtliga åtgärdade i detta jobb:
  - **t7/t10 (kort-anatomi, list- och rutvy):** mockupen visar trenden (▼/▲/±) som en egen halvgenomskinlig pill UPPE TILL VÄNSTER över kortbilden, plus en 3-4px färgton-remsa längst ner på bilden — koden hade istället ingendera, med trenden felaktigt inline bredvid priset i prisscrimmet. `ModelListScreen.kt`: `DeltaPill` ersatt av `TrendBadge` (samma färglogik, ny position+halvgenomskinlig bakgrund), båda `ModelListCard` och `ModelGridCard` fick den nya positioneringen plus en `colorForValueRating`-färgad remsa. `ModelGridCard`s undre textrad bytt från `model.brand` till billigaste källans "pris · butik" (mockupens faktiska innehåll där — märket syns redan på listkortets metarad, ingen information gick förlorad).
  - **t9 (fast footer i detaljvyn):** mockupen har en TVÅ-knapps fast rad (Avslå + Flytta till bevakning, i den ordningen), med "Ta bort setet" liggande i den skrollande ytan (bredvid Verktyg/Fakta), inte i footern. `ModelDetailScreen.kt`s `BottomActionBar` gjordes om till exakt detta (tog bort tredje knappen, `onDelete`-parametern borttagen från signaturen); ny `DeleteModelRow`-composable (oformaterad röd textrad, ingen kant) lagd direkt efter `FactsSection` i båda bredd-lägena (smal och bred, se t11-avsnittet ovan), med SAMMA `model.status == "new"`-synlighetsvillkor och bekräftelsedialog som tidigare — bara en visuell omplacering, ingen ändring av när radering faktiskt är tillgänglig.
  - **Cross-cutting: `AppBackground`-färgen var fel.** `Color.kt`s `AppBackground = 0xFF0D0D0D` (använd som `Scaffold`-`containerColor` i alla fyra skärmar samt `Theme.kt`s `colorScheme.background`) förekom bara i t1:s redan övergivna "Amber Refined"-rond (som chrome på en segmented control, inte en skärmbakgrund) — EVERY konvergerad mockup t5-t10 använder `#0b0d0f` som verklig sidbakgrund, samma värde `ScreenBackground` redan hade. Rättat till `0xFF0B0D0F` (identiskt med `ScreenBackground`) — appens synliga rotbakgrund matchar nu faktiskt designdokumentet på alla skärmar istället för en näraliggande men fel nyans från en övergiven rond.
  - **Medvetet INTE åtgärdat:** (1) t8:s "Fakta"-sektion saknar en fjärde rad ("Kontrollerad", t.ex. "för 2 h") — inget fält i `Model.kt` ger en färdigberäknad relativ tid, och `updatedAt`/`infoUpdatedAt` är råa tidsstämplar vars exakta format (tidszon, `T`/mellanslag-separator) inte är verifierat mot ett riktigt serversvar; att gissa formatet och parsa fel är en sämre risk än att hoppa över en mindre Fakta-rad, så detta lämnades — kräver en verifierad titt på ett riktigt `GET /api/models/<id>`-svar innan det görs. (2) t10:s statusfilter-chip-rad visar bara 3 chips (Alla/Bevakar/Äger) i mockupen mot kodens 5 (plus Sök/Avslagen) — bedömt som mockup-förenkling snarare än en avsiktlig nedskärning (Fas 5:s filter fanns långt innan detta designdokument och är inte skopade mot det), så INTE ändrat; att ta bort Sök/Avslagen-filtren hade varit ett funktionsbortfall ingen bad om. (3) t8:s saknade "⟳ Pris"-knapp i Verktyg är redan känd och listad under "INTE implementerat än" (manuell prisuppdatering från detaljvyn) — inget nytt, ingen ändring behövdes.
- Byggverifiering: `./gradlew assembleDebug` (samma JBR-`JAVA_HOME`-mönster som tidigare) — grön build efter varje steg ovan, inklusive efter t1-t10-fixarna. **Ingen emulator/fysisk enhet-körning denna session** — se "Ej verifierat mot fysisk enhet" nedan, samma begränsning som tidigare Fas-jobb utan tillgänglig `adb devices`-träff.

## Fas 13 — Rutvy/listvy-fix (issue #10 i Sivan87/BrickRadar, 2026-07-29)

Handoff-dokument bifogat till issuen (`design_handoff_grid_list_fix/README.md`) beskrev två separata buggar i `ModelListScreen.kt`, båda åtgärdade i samma jobb:

- **`ModelGrid` sträckte korten på breda skärmar/surfplattor.** Fas 12:s `GridCells.Adaptive(minSize = 160.dp)` (se ovan) växer kolumnantalet obegränsat med bredden utan något tak, vilket på tillräckligt breda skärmar gav färre, bredare (sträckta) kort istället för fler kolumner i konstant bredd. Ersatt med `BoxWithConstraints` + `GridCells.Fixed(columns)`, där `columns = (maxWidth / GRID_CARD_MIN_WIDTH).toInt().coerceIn(1, 4)` — samma `GRID_CARD_MIN_WIDTH`-tröskel som innan, men nu klamrad till max 4 kolumner. Effekt: fler synliga objekt växer med bredden, men korten själva behåller en konstant proportion istället för att sträckas ut.
- **`ModelList` (listvyn) återanvände samma bildkort som rutvyn** (`ModelListCard`, en hel bild+scrim+kr/del-bannerkomponent) istället för en kompakt informationsrad — ingen faktisk skillnad mellan list- och rutvyn förutom kolumnantal. `ModelListCard` borttagen helt (verifierat orefererad utanför denna fil innan borttagning) och ersatt av en ny `ModelListRow`: en 60dp-tumnagel, namn/modellnummer/delantal/kontextnotis i mitten, pris + billigaste källa till höger, en `›`-pil längst till höger — samma highlight-/border-/alpha-logik (baserat på `bestValueRating`/om pris finns) som övriga kort i appen.
- `Brush`-importet (bara använt av `ModelListCard`s gradient-scrim) togs bort som en följdstädning eftersom inget annat i filen längre använder det.
- Byggverifiering: `./gradlew assembleDebug` — grön build. **Ingen emulator/fysisk enhet-körning denna session** — samma begränsning som tidigare Fas-jobb, se "Ej verifierat mot fysisk enhet" nedan.

## Fas 14 — Listvy: bred layout med egen kategori/status-kolumn (design t13a, issue #10, 2026-07-29)

Fas 13 (ovan) täckte designdokumentets rond t13 bara delvis: `ModelListRow` som byggdes då matchar 13b (telefon, 390×844) — en enda mittenkolumn med namn/modellnummer/status klumpat ihop. Rond t13 har dock en andra variant, **13a** (surfplatta/desktop, 1400×900), som bryter ut kategori/status i en EGEN kolumn skild från namnet, med en rubrikrad ("Modell" / "Kategori / status" / "Pris") — den var inte implementerad förrän nu.

- `ModelListRow` är nu en `BoxWithConstraints` som väljer mellan två innehålls-composables efter samma `maxWidth >= 600.dp`-tröskel som `ModelDetailScreen.kt`s tvåspalts-läge (Fas 12): `CompactModelListRowContent` (oförändrad Fas 13-layout, telefon) eller den nya `WideModelListRowContent` (13a: 64dp-bild, "Modell"-kolumn (namn + modellnummer/bitar), egen "Kategori/status"-kolumn, pris-kolumn, `›`-pil — kolumnvikter 2.3f/1.3f/1f speglar mockupens `2.3fr 1.3fr 1fr`-rutnät).
- Kategorietiketten slås upp via en ny `categoryLabel(category, categories)`-hjälpfunktion mot samma `categories: List<Category>` som redan hämtas i `ModelListScreen` (`GET /api/categories`, användes tidigare bara av `FilterBar`) — tråddad genom `ModelList` → `ModelListRow`. `UNCATEGORIZED_KEY` ("ovrigt", samma konstant som `AddModelScreen`/`ModelDetailScreen` redan använder) är fallback för `model.category == null`.
- Status-textens färg i den breda kolumnen (`contextNote(model)`) sätts till `PositiveGreen` när `priceTrend.isAllTimeLow == true` (matchar mockupens gröna "Lägsta pris någonsin" mot grå "Bevakar") — en färgregel som inte fanns i den smala varianten (där hela statusraden alltid var `TextMutedMore`), eftersom 13a:s mockup visar den distinktionen tydligt men 13b inte gör det lika uttalat.
- Pris-kolumnen (belopp + billigaste källa) bröts ut till en delad `PriceColumn`-composable, återanvänd av båda bredd-lägena — ingen duplicerad prislogik.
- Byggverifiering: `./gradlew assembleDebug` — grön build. **Verifierad live** på en ansluten emulator (`emulator-5554`, 2560×1600 @320dpi ≈ 1280dp bredd — triggar automatiskt den breda layouten): skärmdump bekräftar bild/Modell/Kategori-status/Pris i fyra tydliga kolumner, exakt som design t13a, mot en riktig lokal `mould-king-tracker`-server.
- **Medvetet INTE gjort:** ingen fristående verifiering av den smala (13b/telefon-) layouten denna session (samma tablet-emulator användes, ingen telefon-formfaktor-AVD tillgänglig) — koden för det fallet är dock oförändrad sedan Fas 13, som redan verifierades (byggd grönt, om än utan emulator då).

## Fas 15 — Totalpris i fokus i list-/rutvy (issues #11, #12 i Sivan87/BrickRadar, 2026-07-30)

Issue #11/#12 beskrev en webb-redesign (`static/app.js`/`style.css` i `mould-king-tracker`) av samma infohierarki-problem som redan fanns i Android-appen: kr/del visades stort/primärt (guld, `titleMedium`/`titleLarge`), totalpris+källa var en liten sekundär rad — tvärtom mot vad som faktiskt är viktigast dag för dag. Kärnkonceptet (inte webbens exakta detaljer, som kategori-tagg/status-badge — Android-korten har aldrig haft någotdera) porterades till appens `ModelListScreen.kt`:

- `PriceColumn` (delad av `CompactModelListRowContent`/`WideModelListRowContent`): totalpris (`cheapest.totalPriceSek`/`.price`) är nu den stora primära raden (`titleMedium`, `TextPrimary`), kr/del en liten sekundär rad färgad med `colorForValueRating(model.bestValueRating)` istället för det tidigare binära `highlighted`-flaggan (grön vs. grå) — `highlighted`-parametern togs bort helt från funktionssignaturen och båda anropsställena.
- `ModelGridCard`: samma byte — den stora `titleLarge`-siffran är nu totalpris med en ny "TOTALPRIS"-etikett (ersätter "PER BIT"), kr/del flyttat ner till den nedersta raden som en liten `colorForValueRating`-färgad chip bredvid billigaste källans namn (tidigare bara källnamnet). Bildens nedre färgband (`colorForValueRating`, redan sedan tidigare) är oförändrat — det matchade redan webbens overlay-bar-koncept.
- `ListHeader`s "BrickRadar"-wordmark fick `FontWeight.ExtraBold` (tidigare bara `titleLarge`s redan existerande `Bold`) — en pragmatisk motsvarighet till webbens byte till Manrope ExtraBold, utan att lägga till en ny typsnittsresurs (samma medvetna avvägning som `GrotesqueFont`/`MonoFont`-fallbacket i Fas 8).
- **Uppföljning samma dag — användaren klargjorde att webbens fulla omfattning skulle porteras, inte bara prisfokus-bytet:**
  - `ModelGrid`: `GridCells.Fixed(1..4)` (breddberoende, klamrad, se Fas 13) ersatt med ett ovillkorligt `GridCells.Fixed(2)` — rutvyn visar nu alltid max 2 kolumner per rad, oavsett skärmbredd (samma `BoxWithConstraints`/`GRID_CARD_MIN_WIDTH`-breddavkänning behövs inte längre). Matchar webbens `grid-template-columns: repeat(2, 1fr)` exakt istället för att bara vara "inte samma bugg".
  - `ModelListRow`: ny `statusStripeColor(status)` (Sök/`AccentGold`, Bevakar/`StatusWatchingBlue`, Äger/`PositiveGreen`, Avslagen/`TextDim` — samma fyra färger som redan etablerats i `StatistikScreen.kt`s `StatusDistribution`, återanvända för konsekvens snarare än nya). Ritas som en 4dp färgad stripe längst till vänster i raden, i sin fulla höjd (`Row(Modifier.height(IntrinsicSize.Min))` + en `Box.fillMaxHeight().width(4.dp)`, eftersom `BoxWithConstraints` inte har en egen bunden höjd att fylla mot annars) — motsvarar webbens `border-left: 3px solid <statusfärg>` på `.model-list-row`. Verifierat mot riktig serverdata (inte bara mockup-exempeldata) att färgerna faktiskt speglar `model.status` korrekt: en modell med status `"new"` fick guld-stripen, flera med `"owned"` fick den gröna.
- Byggverifiering: `./gradlew assembleDebug` — grön build efter båda rundorna. **Verifierad live** på en ansluten emulator (`emulator-5554`) mot en riktig lokal `mould-king-tracker`-server, båda rundorna: skärmdumpar bekräftar stort totalpris + liten färgad kr/del-chip, exakt 2 rutvy-kolumner (testat på denna breda tablet-emulator som tidigare fick 4 kolumner av Fas 13/14-logiken), och korrekt färgad statusstripe i listvyn.
- **Bakgrund/korrigering:** samma redesign implementerades först (felaktigt, och till en början bara delvis) i webbappen (`mould-king-tracker`, commit `2e41ea2`) innan användaren klargjorde att issue #11/#12 var avsedda för Android — den webbcommiten reverterades (`c31478b`) samma dag.

## Fas 16 — Rutvy/listvy verifierade rad-för-rad mot de faktiska prototypfilerna (uppföljning, 2026-07-30)

Efter Fas 15 rapporterade användaren att listvyn "fortfarande såg ut som gamla designen" och pekade på de tre faktiska `.dc.html`-prototypfilerna (inte bara den tidigare läsningen av GitHub-issuen). En direkt fil-för-fil-jämförelse hittade riktiga strukturella avvikelser i BÅDA vyerna, inte bara en upplevd känsla:

- **Listvy — `PriceColumn`s kr/del-element var färgad TEXT efter `colorForValueRating`, prototypen (`BrickRadar Listvy.dc.html`/`Listvy Options.dc.html`, radlayout 1b) visar en färgad PILL (bakgrund+rundade hörn+mörk text) i SAMMA statusfärg som vänsterkanten (`statusStripeColor`).** Rättat: kr/del är nu en `Box` med `background(statusStripeColor(model.status))` + `AppBackground`-färgad text, inte längre `colorForValueRating`. Detta är den konkreta orsaken till att raden inte kändes "ny" — accentfärgen band inte ihop kant och pris som en enhet.
- **Rutvy — `ModelGridCard` saknade flera element som finns i `BrickRadar Rutvy (nuvarande).dc.html`:**
  - Statuspill (SÖK/BEVAKAR/ÄGER, `statusStripeColor`-bakgrund) uppe till vänster ÖVER bilden — fanns inte alls tidigare (kortet hade ingen statusindikator överhuvudtaget).
  - Totalpris låg tidigare UNDER bilden i vitt (`TextPrimary`) — flyttat till en gradient-scrim OVANPÅ bilden (`Brush.verticalGradient`), färgat `AccentGold` (prototypens `#ffce45`), med "TOTALPRIS"-etiketten kvar ovanför.
  - Märke (`model.brand`, litet/guld/versaler), antal delar (`model.pieceCount` + "delar") och en kontextrad (återanvänder `contextNote(model)`, samma funktion som redan användes i listvyn) — inget av detta visades tidigare på rutkortet.
  - Footer gjordes om till en riktig rad ("Billigast av `model.prices.size`" + källnamn vänster, kr/del `colorForValueRating`-färgad text höger), skild från resten av kortet med en `HorizontalDivider` — matchar `.rv-footer` istället för den tidigare enkla `Row` utan avskiljande linje.
  - Trend-badgen (`TrendBadge`, ▼/▲/±) flyttades från topp-vänster till topp-höger för att göra plats åt den nya statuspillen — prototypens datamodell har ingen trend alls, så positioneringen är en pragmatisk anpassning, inte en direkt kopiering.
- **Medvetet fortfarande INTE pixel-för-pixel:** exakta hex-färger (kortbakgrund `#1e1e1e`, m.m.) — appens egen etablerade färgpalett (Fas 8, "Klonradarn Design Options", en annan/separat designkälla) används fortsatt istället för webbens `#141414`-baserade palett. Det är den strukturella hierarkin/vilka element som finns som jämförts och rättats, inte de exakta hex-värdena.
- Byggverifiering: `./gradlew assembleDebug` — grön build. **Verifierad live** på emulator (`emulator-5554`) mot en riktig lokal `mould-king-tracker`-server: skärmdumpar bekräftar statuspill+guld-totalpris-overlay+märke/delar/kontext/footer i rutvyn, och statusfärgad pill (inte bara text) i listvyn — jämfört sida vid sida mot de tre `.dc.html`-filerna användaren pekade på.

## Implementerat
- Listvy: alla modeller, bild/namn/status/kr-per-del med färgindikator, tryck → detaljvy
- Detaljvy: full modellinfo + alla källor/priser
- Statusändring i detaljvyn: chips för Sök/Bevakar/Äger/Avslagen, sparar direkt via `PATCH /api/models/{id}/status`, laddningsindikator + Snackbar-bekräftelse, återställer till föregående status vid fel
- Redigera modell: pennikon togglar redigeringsläge (namn, delantal, kategori-chips från `GET /api/categories`), validering (namn ej tomt, delantal positivt heltal) blockerar Spara tills korrekt, sparar via `PUT /api/models/{id}`
- Källhantering (Fas 4): lägg till/redigera/ta bort källor i detaljvyn. Redigera-/ta bort-ikon per källrad, "+ Lägg till källa"-knapp, enhetligt bottensheet-formulär (butik/källa [endast vid tillägg — namnet går inte att ändra vid redigering, se append-only-anmärkningen ovan], URL, pris, valuta, lagerstatus-switch, lagerland-chips, leveranstid). Validering: URL och pris obligatoriska, pris måste vara positivt tal (blockerar Spara). Borttagning kräver bekräftelse via `AlertDialog`. Alla tre operationer hämtar om hela modellen från servern efteråt (se append-only-anmärkningen i Arkitektur-avsnittet).
- Felhantering: laddningsindikator, felvy med "Försök igen" vid nätverksfel/401/404, Snackbar-felmeddelande vid misslyckad statusändring/sparning/källhantering (appen kraschar inte, återgår till föregående state, formuläret stängs inte vid fel så användaren kan rätta och försöka igen)
- Filter/kategori/sortering i listvyn (Fas 5): statuschips (Alla/Sök/Bevakar/Äger/Avslagen) och kategorichips (från `GET /api/categories`, inkl. "Alla kategorier") högst upp i listvyn, plus en sorteringsmeny (kr/del lägst-högst, senast ändrad, namn A-Ö, standard). Ändrat filter/sortering triggar nytt `GET /api/models`-anrop med rätt query-parametrar; ett pågående anrop avbryts (`Job.cancel()`) om användaren snabbt växlar filter igen. Föregående lista syns kvar (med en tunn laddningsindikator) under tiden nya resultat hämtas, för mindre flimmer. Tomt filterresultat visar ett tydligt meddelande istället för en tom lista. Filterstate ligger i `ModelListViewModel` och överlever navigation till detaljvyn och tillbaka (samma `ViewModelStore`-scope som `NavHost`:ets `modelList`-destination, verifierat genom att `ModelListViewModel` inte instansieras om vid tillbaka-navigering).
- Lägg till ny modell manuellt (Fas 6): "+"-FAB i listvyn öppnar `AddModelScreen` (formulär: Namn/Modellnummer/Märke/Delantal/Kategori/Status/Bildlänk). Validering: Namn, Märke och Delantal obligatoriska (Delantal måste vara positivt heltal), Spara blockerad tills korrekt. Sparar via `POST /api/models`, med ett uppföljande `PUT /api/models/{id}` om användaren valt en annan kategori än auto-gissningen (se avvikelse-anmärkningen om `category` i Arkitektur-avsnittet). Vid lyckad skapelse: navigerar tillbaka till listvyn, listan hämtas om och en Snackbar ("Modellen har lagts till") visas. Vid serverfel (t.ex. dubblett — 409, "brand krävs" — 400): Snackbar med serverns felmeddelande, formuläret stängs inte och ifyllda värden tappas inte. **Det kända delantal-problemet från webben** (manuellt tillagda modeller tillät bara namnredigering, inte delantal) **är inte relevant för Android** — `piece_count` skickas och sparas korrekt redan vid `POST /models` (bekräftat i `api.py: api_add_model` → `_parse_piece_count`), och efterföljande redigering av delantal gick redan genom `PUT /api/models/{id}` sedan Fas 3, som alltid tillåtit delantal. Inget appspecifikt workaround behövdes för detta.
- Brick4-sökning vid modelltillägg (Fas 7): `AddModelScreen` har nu ett läge-val, "Sök modellnummer" (default) eller "Fyll i manuellt" (oförändrat Fas 6-flöde). I sökläget: skriv ett modellnummer, tryck sök → `GET /api/brick4/search-by-number` (befintligt backend-endpoint, ingen ändring i `mould-king-tracker`, se avvikelse-anmärkningen i Arkitektur-avsnittet). Noll träffar: felmeddelande + "Fyll i manuellt istället"-knapp som byter läge med numret redan ifyllt. Ett märke: visas direkt som ett kort med "Använd denna". Flera märken: alla visas som kort, användaren väljer rätt. Efter val: modellnummer+märke låses in, resten av fälten (namn/delantal/kategori/status/bildlänk) är samma som det manuella formuläret men namn/delantal är VALFRIA — lämnas de tomma fylls de i automatiskt av samma asynkrona bakgrundshämtning som redan körs för alla nya modeller med känt modellnummer (`initial_fetch`), precis som webbens motsvarande formulär. Uppföljande kategori-PUT (om vald kategori ≠ auto-gissning) skickar nu BARA category-fältet (`CategoryUpdateRequest`) för att inte riskera att skriva över namn/delantal bakgrundshämtningen redan hunnit sätta. **Ingen förhandsvisning av namn/bild/delantal innan modellen skapas** (till skillnad från kickoff-dokumentets ursprungliga vision) — se den utförliga avvikelse-anmärkningen i Arkitektur-avsnittet för varför (backend-svaret innehåller inte den datan förrän efter skapande).
- Visuell redesign (Fas 8): mörkt tema, list-/rutvy med bottennav, detaljvyns värde-skala och kontextuella åtgärdsrad, samt borttagning av hel modell — se Arkitektur-avsnittets Fas 8-punkt för detaljer och medvetna avgränsningar (typsnitt, foldbar-stöd).
- Auto-update (Fas 9): appen kollar GitHub Releases (sedan 2026-07-28 — se Fas 9-avsnittet ovan) en gång vid varje appstart, visar en dialog ("Ny version tillgänglig") om senaste release har en högre `versionCode`, laddar ner APK:n via `DownloadManager` och startar installations-intentet (med guide till "installera okända appar"-inställningen om behörigheten saknas). Fungerar oavsett vilket nätverk telefonen är på, inte bara hemma-WiFi.
- Statistik-flik (Fas 10, version 1): bottennavens "Statistik"-flik (tidigare inaktiv "Priser") navigerar till en egen skärm som visar övergripande kr/del-snitt för Klon respektive LEGO — se Fas 10-avsnittet ovan för detaljer och medvetna avgränsningar.
- Statistik-flik fullt paritet + värdeskale-fix + borttagen dödknapp (Fas 11, issues #4/#5/#6): se Fas 11-avsnittet ovan. `StatistikScreen` visar nu allt `static/app.js: renderStats()` visar (statusfördelning, sex kort, Klon-/LEGO-snitt, värdefördelning, bästa fynd, märkes-/kategorifördelning, med navigation till modelldetalj). `ModelDetailScreen`s värdeskala färgas nu efter samma fasta cyan/grön/gul/orange/röd-trösklar som resten av appens kr/del-färgsättning (`util/ValueRating.kt`), istället för en relativ 3-färgsgradient. Den överflödiga, icke-klickbara statuspillen bredvid VÄRDE/BIT är borttagen.

## INTE implementerat än
- MOC-hantering (utöver att kunna markera en modell som märke "Generic"/utan modellnummer vid tillägg), bilduppladdning (bara bildlänk/URL stöds)
- VPN-stöd / inställningsskärm för server-IP (se ovan — hårdkodat med avsikt)
- Manuell prisuppdatering (`POST .../refresh`) från detaljvyn
- Förhandsvisning av namn/bild/delantal INNAN en modell skapas via Brick4-sökning (Fas 7 mirrorar webbens beteende — se avvikelse-anmärkningen i Arkitektur-avsnittet — snarare än att bygga ett nytt backend-endpoint för detta; skulle kunna läggas till senare om det visar sig behövas)
- Foldbar-stöd (design-dokumentets rond t11 — tvåpanelsläge på breda/uppfällda skärmar), riktiga Google Fonts-typsnitt (se Fas 8-anmärkningen om `GrotesqueFont`/`MonoFont`), en riktig skärm bakom bottennavens "Mer"-flik (fortsatt medvetet inaktiv — "Statistik" fick sin skärm i Fas 10, se ovan)
- Exakt nedladdningsprocent för auto-update (Fas 9 — bara `DownloadManager`s inbyggda notis + en obestämd in-app-indikator, se Fas 9-avsnittet), periodisk omkoll medan appen redan är öppen (bara vid appstart), manuell "kolla efter uppdatering nu"-knapp

## Ej verifierat mot fysisk enhet
Kodet i Fas 3 (statusändring, redigering), Fas 4 (källhantering), Fas 5 (filter/sortering), Fas 6 (lägg till modell), Fas 7 (Brick4-sökning vid modelltillägg), Fas 8 (visuell redesign) och Fas 9 (auto-update) är kompilerat och `assembleDebug` går igenom, men INTE körd på emulator/fysisk telefon av Claude (`adb devices` gav en tom lista tidigare sessioner, ingen AVD konfigurerad). — se `TODO.md` för kvarstående manuell verifiering (spara-mot-server, samtidig kontroll i webb-UI:t, felhantering med avstängd server, Fas 4:s specifika verifieringspunkter kring append-only-beteendet och leveranstid-overriden, Fas 5:s filter-/sorterings-/navigationsverifiering, Fas 6:s verifieringspunkter kring kategori-avvikelsen, dubblettkontrollen och avbryt-flödet, Fas 7:s verifieringspunkter kring 0/1/flera-träffar-hantering, bakgrundshämtningen av namn/bild/delantal efter en sökskapad modell och konsekvens mot webb-UI:t, Fas 8:s verifieringspunkter kring det faktiska typsnitts-fallbacket på enhet, värde-skalans layout på olika skärmstorlekar, och att modellborttagning faktiskt tar bort raden server-side, samt **Fas 9:s verifieringspunkter**: att uppdateringsdialogen faktiskt dyker upp när servern har en högre `versionCode`, att appen INTE kraschar/visar en felruta när servern är onåbar (fail-silent-kravet), att "installera okända appar"-behörighetsflödet (både redan beviljad och nekad-vid-start) faktiskt leder till en fungerande installation, och att uppdateringsdialogen INTE dyker upp igen efter att den nya versionen installerats. **App-ikonen** (Fas 8) är verifierad genom att packa upp den signerade APK:n och kontrollera den kompilerade `ic_launcher.xml` med `aapt2 dump xmltree` (pekar korrekt mot de nya mipmap-/färgresurserna) — men INTE genom faktisk installation. Visuell kontroll av kugghjulet i launcher-listan (rund/kvadratisk maskform, inget avklippt) återstår.

**Fas 10 och Fas 11 ÄR verifierade mot en riktig körande emulator** (2026-07-28, `emulator-5554` var ansluten och en `adb devices` gav en riktig träff denna session, till skillnad från tidigare fällor — se ovan) — `assembleDebug`, `adb install -r`, och skärmdumpar via `adb exec-out screencap`, mot en riktig lokal `mould-king-tracker`-server: statusfliken navigerar dit/tillbaka korrekt, värdeskalans nya segmenterade färgband och "Detta set"-textens färg stämde mot modellens egna `best_value_rating` (verifierat på modell "Ford GT" #10651, samma modell som i issue #4:s buggrapport), den gamla SÖK-pillen är borta (issue #5), och `StatistikScreen`s alla siffror (statusräkningar, sex kort, båda snitten, värdefördelning, fem bästa-fynd-rader, nio märkesrader, fem kategorirader) jämfördes fält-för-fält mot ett rått `GET /api/stats`-curl-anrop och stämde exakt, inklusive att en klickad bästa-fynd-rad navigerar till rätt modells detaljvy (issue #6).

## Release build
- **Keystore:** `C:\Keys\brickradar-release.jks` (alias `brickradar`) — genererad manuellt med `keytool -genkeypair`, ligger MEDVETET utanför repot (`C:\Brickradarapp`) så den aldrig kan committas av misstag. Innehåller inte bara appens identitet utan är den ENDA vägen att signera framtida uppdateringar med samma signatur — se varningen nedan.
- **Signeringskonfiguration:** `app/build.gradle.kts` läser `RELEASE_STORE_FILE`/`RELEASE_STORE_PASSWORD`/`RELEASE_KEY_ALIAS`/`RELEASE_KEY_PASSWORD` från root-projektets `gradle.properties` via `project.findProperty(...)` (Gradle exponerar automatiskt rot-`gradle.properties` som projekt-properties för alla subprojekt, ingen manuell fil-parsing behövdes) och kopplar in dem i `signingConfigs.create("release")` → `buildTypes.release.signingConfig`.
- **`gradle.properties` innehåller lösenorden i klartext lokalt** — filen (samt alla `*.jks`/`*.keystore`) står i `.gitignore` och ska ALDRIG committas. Claude satte bara in platshållarvärden (`REPLACE_ME`) för lösenorden; användaren fyllde i de riktiga lösenorden själv direkt i filen så de aldrig behövde skickas genom chatten.
- **Bygg:** `./gradlew assembleRelease` (kräver `JAVA_HOME` satt — Android Studios bundlade JBR funkar, t.ex. `C:\Program Files\Android\Android Studio\jbr`, om ingen annan JDK finns på PATH). Resultat: `app/build/outputs/apk/release/app-release.apk`.
- **Verifierad signerad** (2026-07-27) med `apksigner verify -v --print-certs` (`%LOCALAPPDATA%\Android\Sdk\build-tools\<version>\apksigner.bat`) — v2-signaturschema, signerarens DN matchar keystoren (`CN=BrickRadar, OU=Dev, O=Sivan, L=Unknown, ST=Unknown, C=SE`).
- **VIKTIGT — samma keystore måste användas vid ALLA framtida uppdateringar av appen.** Tappas `brickradar-release.jks` eller dess lösenord bort går appen bara att avinstallera och installera om på nytt (ingen uppdateringsväg, all lokal data i appen förloras). Säkerhetskopiera filen någonstans utanför den här datorn.
- **Installation på telefon:** överför `app-release.apk` (Google Drive/e-post/USB), öppna filen och tillåt "installera okända appar" för den app som öppnar den.
- **Automatisk versionshöjning:** `versionCode`/`versionName` höjs numera automatiskt av `app/build.gradle.kts` vid `assembleRelease` — ingen manuell redigering i `build.gradle.kts` behövs längre. Enda källan till sanning är `version.properties` i projektroten (`VERSION_CODE=`/`VERSION_NAME=`, committas till git, till skillnad från `gradle.properties`). Build-scriptet läser filen vid varje Gradle-körning; bara om något av de begärda tasknamnen innehåller "Release" (dvs. `assembleRelease`, men även `bundleRelease`/`lintRelease` osv.) höjs `VERSION_CODE` med +1 och skrivs tillbaka, med `VERSION_NAME` satt till `"1.$VERSION_CODE"`. `assembleDebug` läser bara det aktuella värdet, höjer aldrig. Vill man se det nya versionsnumret utan att gissa: `cat version.properties` efter en `assembleRelease`-körning.

## Så här uppdaterar du appen (GitHub-integration, sedan 2026-07-28; direkt-mot-GitHub sedan samma datum, issue #1)
Release-processen är automatiserad end-to-end via GitHub Actions — se
`brickradar-github-integration.md` för hela kickoff-specen (Del A–D). Sedan
issue #1 i Sivan87/BrickRadar (samma dag, ett uppföljande arbete) är repot
publikt och appen pratar med GitHub Releases API direkt — `mould-king-tracker`
(servern) är inte längre inblandad i uppdateringskollen alls, se Fas 9-avsnittet
ovan.

1. **Committa och pusha till `main`** som vanligt (`git push origin main`).
   Det är det ENDA manuella steget.
2. `.github/workflows/release.yml` triggas av pushen och gör resten
   automatiskt: höjer `VERSION_CODE`/`VERSION_NAME` i `version.properties`,
   bygger en signerad release-APK (keystore/lösenord från repots Actions
   Secrets, se Del A i kickoff-specen), committar tillbaka den höjda
   `version.properties` till `main` (med `[skip ci]` så det inte triggar en
   ny körning), och skapar en GitHub Release taggad `v<versionName>` med
   APK:n som asset och senaste (den riktiga, triggande) commit-meddelandet
   som release notes.
3. `app/build.gradle.kts`s lokala auto-höjningslogik (se "Release build" ovan)
   hoppar nu över sig själv när `CI=true` (satt automatiskt av Actions) — den
   höjer/skriver alltså bara `version.properties` vid en LOKAL
   `assembleRelease`, aldrig i CI, så versionen dubbelhöjs inte.
4. Appen själv (inte servern, se ändringen 2026-07-28 ovan) hämtar senaste
   release direkt från `GET https://api.github.com/repos/Sivan87/BrickRadar/releases/latest`
   (ingen autentisering, publikt repo) nästa gång den startar — oavsett vilket
   nätverk telefonen är på, inte bara hemma-WiFi som tidigare. Ingen manuell
   publiceringshandling behövs någonstans.

**Förstagångsinstallation** (ingen tidigare version installerad än) görs
fortfarande manuellt: ladda ner APK:n från GitHub-releasens assets och öppna
filen på telefonen.

**`mould-king-tracker`s gamla `GET /api/app-version`-endpoint, `publish-update.py`
och `version.json` är borttagna** (issue #1, 2026-07-28) — de var en nödlösning
för när repot var privat (appen kunde då inte anropa GitHub direkt utan en PAT,
som bara servern hade). Nu när repot är publikt behövs mellanhanden inte
längre. `GITHUB_RELEASES_TOKEN` i `mould-king-tracker/.env` är av samma skäl
borttagen/inte längre använd av något — kan revokeras på GitHub om den inte
redan är det.

**Viktigt:**
- Signeras APK:n med FEL keystore vägrar Android uppdatera och kräver
  avinstallation först (all lokal appdata förloras då) — därför måste alltid
  samma keystore-fil (`C:\Keys\brickradar-release.jks`, base64-kodad i Actions-
  secreten `KEYSTORE_BASE64`) återanvändas, aldrig en nygenererad.
- En push till `main` triggar workflowet oavsett vilken fil som ändrades (ingen
  path-filtrering) — precis som kickoff-specen (Del C) anger.
